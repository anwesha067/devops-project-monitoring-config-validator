apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitoring-validator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: validator
  template:
    metadata:
      labels:
        app: validator
    spec:
      containers:
      - name: validator
        image: monitoring-config-validator